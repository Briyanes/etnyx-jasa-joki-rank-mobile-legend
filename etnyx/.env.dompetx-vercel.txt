# ============================================================
# DompetX Migration — Vercel Environment Variables
# Copy these into Vercel → Project → Settings → Environment Variables
# Production domain: etnyx.com
# ============================================================

# --- DompetX Payment Gateway (Basic Merchant v2) ---
DOMPETX_API_KEY=dompk_your-api-key-here
DOMPETX_BASE_URL=https://api.dompetx.com/v1

# Webhook URL to register in DompetX Dashboard:
# https://etnyx.com/api/payment/notification

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --- Admin Authentication ---
ADMIN_EMAIL=admin@etnyx.com
ADMIN_PASSWORD_HASH=$2a$10$...your-bcrypt-hash...
ADMIN_JWT_SECRET=generate-a-random-64-char-secret

# --- Customer Authentication ---
JWT_SECRET=generate-a-random-64-char-secret

# --- Encryption ---
ENCRYPTION_KEY=your-32-character-encryption-key!

# --- Email (Resend) ---
RESEND_API_KEY=your-resend-api-key
FROM_EMAIL=noreply@etnyx.com

# --- Push Notifications (VAPID) ---
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key

# --- Site Configuration ---
NEXT_PUBLIC_SITE_URL=https://etnyx.com
NEXT_PUBLIC_SITE_NAME=ETNYX
NEXT_PUBLIC_WHATSAPP_NUMBER=628xxxxxxxxxx

# --- Cron (Vercel Cron Jobs) ---
CRON_SECRET=generate-a-random-secret

# --- DompetX Webhook Security (K2) ---
# Shared secret for webhook verification. Set in DompetX dashboard too.
# Generate with: openssl rand -hex 32
DOMPETX_WEBHOOK_SECRET=generate-a-random-webhook-secret

# --- Analytics & SEO ---
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
GOOGLE_SITE_VERIFICATION=your-verification-code